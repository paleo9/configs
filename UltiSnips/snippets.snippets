snippet file "Class declaration template" b
<?php

namespace ${1:`!p
relpath = os.path.relpath(path)
m = re.search(r'[A-Z].+(?=/)', relpath)
if m:
    snip.rv = m.group().replace('/', '\\')
`};

/**
 * @File ${1:`!p snip.rv=snip.basename`}
 * @author ${2:`!v g:snips_author`}
 */
}
endsnippet
